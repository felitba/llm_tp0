/.venv
venv/
__pycache__/
*.py[cod]
*.pyc
.env
/.idea/
.DS_Store

# Every generated artifact lands here: experiment runs, figures, EDA, reports.
# Source never does. The old `graph/` rule ignored a directory that held both,
# so the plotting code was invisible to git along with its .jpg output.
/output/

# Local-only notes and scratch.
dataset/test_preprocessing.py
# Scratch dir; unrelated to the tracked baselines/ (BERT comparison).
baseline/
comentarios_para_el_equipo/extra/
docs/INFORME.md
docs/PROTOCOL.md
.gitignore
docs/PROTOCOL.md
metrics/final_table.py
plots/error_patterns_figure.py
CLAUDE.md
