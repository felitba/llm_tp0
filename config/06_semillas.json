{
  "_step": "06",
  "_comment": "PASO 6 - Barras de error sobre el modelo final. Sin esto ningun gap de los pasos anteriores se puede defender contra 'eso no es ruido?': en s1_solo_title_tag el val_pr_auc oscilaba entre 0.633 y 0.696 entre epocas del MISMO modelo. Cambia solo la semilla de inicializacion; split_seed queda fijo en 42 para que las tres corridas vean exactamente la misma particion y la unica fuente de variacion sea el entrenamiento. Reporta media y rango, no el mejor de los tres.",
  "_carry_forward": "Del paso 5: la configuracion final completa.",
  "dataset_path": "dataset/supermarket_products.csv",
  "output_dir": "output/06_semillas",
  "d_model": 96,
  "vocab_size": 50257,
  "n_heads": 4,
  "num_layers": 2,
  "dim_feedforward": 192,
  "learning_rate": 0.0003,
  "epochs": 50,
  "train_split": 0.8,
  "validation_split": 0.1,
  "test_split": 0.1,
  "split_seed": 42,
  "seed": 42,
  "dropout": 0.3,
  "batch_size": 64,
  "weight_decay": 0.02,
  "early_stopping_patience": 0,
  "early_stopping_min_delta": 0.0001,
  "early_stopping_metric": "val_pr_auc",
  "activation": "relu",
  "encoding": "gpt2",
  "drop_columns": [
    "title",
    "description",
    "filter_category",
    "filter_storage_type",
    "package_size",
    "unit_of_measure",
    "dimensions_in",
    "timestamp",
    "cart",
    "ingredients"
  ],
  "normalize_columns": [
    "price",
    "filter_price_min",
    "filter_price_max",
    "net_weight_oz",
    "nutrition_score"
  ],
  "categorical_columns": [
    "category",
    "allergens",
    "title_tag",
    "brand",
    "storage_type",
    "country_of_origin"
  ],
  "numeric_columns": [
    "price",
    "nutrition_score",
    "filter_price_min",
    "filter_price_max",
    "net_weight_oz"
  ],
  "text_column": [
    "product_name"
  ],
  "max_title_len": 10,
  "experiments": [
    {
      "name": "s6_seed_42",
      "_comment": "",
      "overrides": {
        "seed": 42
      }
    },
    {
      "name": "s6_seed_7",
      "_comment": "",
      "overrides": {
        "seed": 7
      }
    },
    {
      "name": "s6_seed_1234",
      "_comment": "",
      "overrides": {
        "seed": 1234
      }
    }
  ]
}
